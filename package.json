{
  "name": "Representer",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "flow-remove-types src/ -d lib/",
    "prepublish": "yarn run build",
    "start": "flow-remove-types src/ -d lib/ && node lib/index.js",
    "test": "flow-remove-types src/ -d lib/ && jest",
    "testDebug": "flow-remove-types src/ -d lib/ && node --inspect-brk node_modules/.bin/jest --runInBand"
  },
  "author": "Vasily Tokarev <vasily.t@icloud.com>",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "cheerio": "^1.0.0-rc.2",
    "flow-bin": "^0.61.0",
    "flow-remove-types": "^1.2.3",
    "jest": "^22.0.4",
    "marked": "^0.3.9"
  }
}
